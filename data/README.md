# Data

Place only small documentation files here. Do not commit raw MRI volumes.

Recommended local-only layout:

```text
data/
  raw/
    prostate158_train/
```

The scripts can also read the dataset from another location, such as `E:/prostate158_train/prostate158_train`, through `configs/paths.json`.
